Rust must be used where perfomance and security matter the most and stuff must be low-level
Zig must be used to implement the QuanticScript programming language ( given it will be implemented in this version of the blockchain )
Elixir must be used where a lot of things must be handled in parallel and a lot of fault tolerance is needed yet being high-level is not a problem

The majority of the project must be written in Rust
